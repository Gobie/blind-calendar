@import './common.styl'

.calendar-item
  padding 0.5em 0

  .time
    background-color $back-light-color
    padding .5em

  .event-content
    white-space pre
    padding .5em

  &:active, &:focus, &.selected
    .time
      background-color $main-color
      color #fff
