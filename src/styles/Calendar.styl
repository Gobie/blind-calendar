@import './common.styl'

header
  background-color $main-color
  padding 0.1em 1em
  @media (min-width: $screen-sm-min)
    padding 0.5em 3em
  color #FFF
  margin 1em 0 0 0

header > h1 > .glyphicon
  margin-right 0.25em

nav
  padding 0.5em 0
  border-bottom 2px solid $main-color

.content
  padding-top 1em
